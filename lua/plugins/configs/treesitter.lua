local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "latex", "scala" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
